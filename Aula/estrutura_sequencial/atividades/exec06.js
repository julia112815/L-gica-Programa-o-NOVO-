//Faça um algorito para ler duas variáveis A e B, e efetuar as trocas dos valores de forma que a variável A passe a possuir o valor da variável B e a Variável B passe a possuit o valor da Variável A. Apresenta na tela dois valores trocados ( antes AB, depois AB).

let A, B 

A =parseFloat(prompt(`${A}, Digite o Valor A `)) 
B =parseFloat(prompt(`${B}, Digite o Valor B `))

document.write(`O valor da variável B é ${A} e da Variável A é ${B}<br>`)

document.write(` O valor da variável B é ${B} e da Variável A é ${A}<br>`)